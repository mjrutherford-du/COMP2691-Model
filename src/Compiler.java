/** Class representing a compiler -- a program that converts text-based source code into
 * assembly code.
 */
public class Compiler
{
    public AssemblyCode compile(String sourceCode)
    {
	return new AssemblyCode();
    }
}
